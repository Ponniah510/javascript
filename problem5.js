/*Create a function which takes an array as a parameter and prints out each element in the array*/

function printArrayElements(arr) {
    for (let i = 0; i < arr.length; i++) {
      console.log(arr[i]);
    }
  }
  const myArray = [1, 2, 3, 4, 5];
  printArrayElements(myArray);